server:
  port: 8080

#eureka:
#  instance:
#    instanceId: ${spring.application.name}:${spring.application.instance_id:${random.value}}
#  client:
#    serviceUrl:
#      defaultZone: http://10.100.152.53:8761/eureka/

spring:
  application:
    name: wesley-rabbitmq
  rabbitmq:
    host: localhost
    prot: 5672
    username: guest
    password: guest